---
import { getCollection } from "astro:content";
import PageLayout from "@layouts/PageLayout.astro";
import { SITE, BLOG } from "@consts";

// 获取博客文章
const blog = (await getCollection("blog"))
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 5); // 直接写数字，避免 Site 类型报错
---

<PageLayout title="Home" description={SITE.DESCRIPTION}>
  <div class="container mx-auto px-4 py-12">
    <section class="space-y-12">
      
      <div class="space-y-4">
        <h1 class="text-4xl font-black tracking-tighter text-black dark:text-white uppercase">
          {SITE.TITLE}
        </h1>
        <p class="text-lg text-neutral-600 dark:text-neutral-400 max-w-2xl">
          {SITE.DESCRIPTION}
        </p>
      </div>

      <hr class="border-neutral-200 dark:border-neutral-800" />

      <div class="space-y-8">
        <div class="flex justify-between items-end">
          <h2 class="text-2xl font-bold">Latest Reality Checks</h2>
          <a href="/blog" class="text-sm underline underline-offset-4 hover:text-black dark:hover:text-white transition-colors">
            View all posts
          </a>
        </div>
        
        <ul class="flex flex-col gap-8">
          {blog.map(post => (
            <li class="group">
              <a href={`/blog/${post.slug}`} class="block">
                <div class="flex flex-col gap-2">
                  <span class="text-sm text-neutral-500">
                    {post.data.date.toLocaleDateString("en-US", {
                      year: "numeric",
                      month: "short",
                      day: "numeric",
                    })}
                  </span>
                  <h3 class="text-xl font-bold group-hover:text-neutral-500 transition-colors">
                    {post.data.title}
                  </h3>
                  <p class="text-neutral-600 dark:text-neutral-400 line-clamp-2">
                    {post.data.description}
                  </p>
                  <span class="text-sm font-semibold uppercase tracking-wider underline underline-offset-4">
                    Read Post →
                  </span>
                </div>
              </a>
            </li>
          ))}
        </ul>
      </div>

    </section>
  </div>
</PageLayout>